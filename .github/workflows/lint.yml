name: Lint

on:
  push:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  clippy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Run clippy
        run: cargo clippy --locked -- -D warnings
